% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte_BUDS.R
\name{tte_BUDS}
\alias{tte_BUDS}
\title{Robust Restricted Kwak-Jung Design under BUDS}
\usage{
tte_BUDS(
  S0L = NULL,
  S0U = NULL,
  S1 = NULL,
  x0,
  rate,
  S0 = NULL,
  lambda0 = NULL,
  lambda0L = NULL,
  lambda0U = NULL,
  HR = NULL,
  lambda1 = NULL,
  alpha = 0.05,
  beta = 0.1,
  n.ub = 150L,
  n_design_points = 21L,
  n_eval_grid = 21L,
  nsim_eval = 5000L,
  BUDS_objective = c("avg_en", "worst_regret"),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{S0L, S0U}{Null survival interval at \code{x0}. If both are omitted, the
function returns only the classic r-KJ design evaluated at \code{S0}.}

\item{S1}{Target survival at \code{x0}. Supply either \code{S1} or
\code{HR}.}

\item{x0}{Follow-up window.}

\item{rate}{Accrual rate.}

\item{S0}{Planning survival probability at \code{x0} used to construct the
classic comparator \code{"r-KJ"} design.}

\item{lambda0}{Optional planning hazard rate at \code{x0}; converted
internally to \code{S0 = exp(-lambda0 * x0)}.}

\item{lambda0L, lambda0U}{Optional null hazard-rate benchmark range at \code{x0}. If
supplied, these are converted internally to \code{S0L/S0U}; when
\code{S0} is omitted, the planning value is derived from the midpoint
hazard rate.}

\item{HR}{Hazard ratio \eqn{\lambda_0 / \lambda_1}. If provided, \code{S1}
is derived internally as \eqn{\exp\{\log(S0) / HR\}}.}

\item{alpha}{One-sided type I error. Default 0.05.}

\item{beta}{Type II error. Default 0.10.}

\item{n.ub}{Maximum total sample size considered.}

\item{n_design_points}{Number of shared grid points used for candidate
generation and interval evaluation.}

\item{n_eval_grid}{Legacy compatibility argument; ignored in favor of
\code{n_design_points}.}

\item{nsim_eval}{Legacy compatibility argument retained for API stability.}

\item{BUDS_objective}{BUDS objective(s) used to select winner(s). Publicly
supported defaults are \code{"avg_en"} and \code{"worst_regret"}.}

\item{verbose}{Print progress. Default FALSE.}

\item{...}{Additional arguments passed to the internal local r-KJ search and the
C++ search engine (\code{ceps}, \code{alphaeps}, \code{nbmaxiter}).}
}
\value{
An object of class \code{"tte_BUDS"} with:
\describe{
\item{designs}{Numeric matrix of selected designs, one column per design.}
\item{inputs}{List of user inputs and resolved defaults.}
\item{meta}{Execution metadata.}
}
}
\description{
Searches for r-KJ designs robust across a benchmark range for survival at
\code{x0}. The returned object mirrors the top-level structure of
\code{\link{binary_BUDS}}, with \code{designs}, \code{inputs}, and
\code{meta} components.
}
\details{
The public interface uses survival notation \code{S0}, \code{S0L},
\code{S0U}, and either \code{S1} or \code{HR}.
}
\examples{
\dontrun{
d <- tte_BUDS(
  S0 = 0.49, S0L = 0.48, S0U = 0.52, S1 = 0.707,
  x0 = 1, rate = 15, n.ub = 100
)
d
}

}
